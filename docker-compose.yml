version: '3'
services:
  app:
    container_name: happynewyear
    restart: always
    build: .
    ports:
      - '8080:8080'
      - '443:443'
    volumes:
      - ./:/app
       - /usr/src/app/my-app/node_modules
